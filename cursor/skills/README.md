# Cursor Skills (experiments)

[Agent Skills](https://cursor.com/docs/context/skills) — each subfolder is one skill with a `SKILL.md`.
Symlinked to `~/.cursor/skills`. Experiment here; promote to org repos (e.g. `.cursor/skills/` in monorama) when ready.
