# User subagents

[User subagents](https://cursor.com/docs/context/subagents) — add `.md` files with YAML frontmatter (name, description).  
Symlinked to `~/.cursor/agents`. Available in all projects.
